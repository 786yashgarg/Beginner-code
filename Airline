#include <bits/stdc++.h>
using namespace std;

int main() {
	int t;
	cin>>t;
	while(t>0)
	{t--;
	int x,n;
	cin>>x>>n;
	float p=(n/100)-x;
	
	if(p>=0)
	{if(n%100==0)
	cout<<p<<endl;
	else
	cout<<p+1<<endl;}
	
	else
	cout<<0<<endl;}
	return 0;

}
