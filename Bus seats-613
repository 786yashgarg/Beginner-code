include <bits/stdc++.h>

using namespace std;

int main() {
    int t;
    cin >> t;
    while (t > 0)
    {
        t--;
        int x;
        cin >> x;
        if (x < 11)
            cout << "lower double" << endl;
        else if (x >= 11 && x < 16)
            cout << "lower single" << endl;
       else if (x >= 16 && x < 26)
            cout << "upper double" << endl;
        else
            cout << "upper single" << endl;
    }
    return 0;

}
